import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.johnkayfundz.techstorepro',
  appName: 'TechStore Pro',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
